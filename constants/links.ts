/**
 * External URLs the app opens. One place, so a screen never carries a literal URL and the
 * address of a published document changes in exactly one line.
 */

/**
 * The published privacy policy (issue #202). Built from `docs/privacy-policy.md` by
 * `.github/workflows/privacy-policy-pages.yml` on every push to `main`; the Settings screen's
 * "Full privacy policy" row opens it (captain-certified 2026-09-19).
 */
export const PRIVACY_POLICY_URL = 'https://ianqiu979.github.io/Ai-Running-Form-Analysis-App/privacy-policy/';
